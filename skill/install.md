# Install

Copy this directory to `~/.claude/skills/mondayctl/` to make the skill available in all projects:
`mkdir -p ~/.claude/skills && cp -r skill ~/.claude/skills/mondayctl` — or copy it to `.claude/skills/mondayctl/` inside a project for project-only use.
